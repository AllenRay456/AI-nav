import { chinaAiProductTop50 } from "@/config/rank"
import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableFooter,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table"

export const metadata = {
  title: '国产AI工具排行榜Top50',
  description: '最新的国产 AI 工具排行榜 Top50已经发布，内容涵盖国内最热门的 AI 工具，包括稳定前列的文心一言，后来居上的抖音豆包，专业赛道的devv.ai	，初出茅驴的涂鸦 AI'
}

# 最新国产AI工具排行榜Top50

    <Table>
      <TableCaption> 引用 AI产品榜（官网：aicpb.com）</TableCaption>
      <TableHeader>
        <TableRow>
          <TableHead className="w-20">排行</TableHead>
          <TableHead>产品名</TableHead>
          <TableHead>分类</TableHead>
          <TableHead>5月预估流量（Visits）</TableHead>
          <TableHead>相对上月变化</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {chinaAiProductTop50.map((data) => (
          <TableRow key={data.rank}>
            <TableCell className="font-medium">{data.rank}</TableCell>
            <TableCell className="prose prose-slate">{data.product}</TableCell>
            <TableCell>{data.category}</TableCell>
            <TableCell>{data.visits}</TableCell>
            <TableCell>{data.change}</TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
